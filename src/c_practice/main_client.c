#include <stdio.h>
#include "math_helper.h"

int main()
{
    int addResult = add(10, 15);
    printf("%d", addResult);
    // printf("%d", subtract(25, 5));
    // printf("%d", multiply(2, 5));
    // printf("%f", divide(40, 5));
    return 0;
}