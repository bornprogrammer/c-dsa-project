#include <stdio.h>

// int calculateFactorialIteratively(int val);

// int calculateFactorialRecursively(int val);

int calculateFactorialIteratively(int val)
{
    printf("hello iteratively");
    return 0;
}

int calculateFactorialRecursively(int val)
{
    printf("hello recursively");
    return 0;
}

int main()
{
    calculateFactorialRecursively(000);
    return 0;
}